import React from 'react'

export const register = (form) => {
    console.log(form);
    //save in db 
}

const checkAns = (startTime,submittedAt,ans) => {
    //verify ans if its right return true or false if wrong and its true then store it in db
    
}
